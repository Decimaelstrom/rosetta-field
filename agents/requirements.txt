# LLM API clients
openai>=1.0.0
anthropic>=0.7.0

# HTTP clients for local LLM support
aiohttp>=3.8.0
requests>=2.28.0

# Optional: Additional local LLM clients
# ollama-python>=0.1.0

# Core dependencies (should already be available)
# asyncio (built-in)
# dataclasses (built-in)
# typing (built-in)
